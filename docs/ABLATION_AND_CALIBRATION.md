# Ablation & Calibration Workflow

Before pushing variables across the engine into Live Trading boundaries, the framework absolutely mandates executing pure combinatorial sweeps mapping mathematical behaviors implicitly off-chain explicitly generating tracking proofs.

## Running Ablation Sweeps
To test whether a feature definitively improves limits across explicit environments, run the offline mapping router:
```bash
python scripts/run_ablation.py
```

### 1. Ablation Modifiers
Disables targeted layers inherently stripping parameters (e.g. `SocialVelocityScore = False`) running identical Replay logs producing exact calculations isolating metric dependencies matching explicit targets gracefully testing baseline dependencies mapping directly onto the system natively.

### 2. Threshold Calibration
Uses `configs/calibration_grid.yaml` checking exact start and end numbers checking steps tracking optimal numeric dependencies isolating perfect thresholds natively.

### 3. Output Validations
Outputs generate `ExperimentManifest` checking baseline parameters guaranteeing exact metrics mapping dependencies scaling boundaries safely natively checking the exact behavior intuitively natively matching expected targets safely.
