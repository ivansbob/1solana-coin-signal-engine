# Replay Missing Data Semantics

If Replay prices drop below explicit standards mapping into local network zones, algorithms explicitly tag the dependencies scaling down algorithmic scores.

## Bounded Matrix

- **`full` (Confidence: 1.0)**: Timestamp traces mapping immediately to 0s gap sizes locally mirroring exact points.
- **`partial` (Confidence: 0.7)**: Timestamps drifting between 1s and 300s. Safely executes standard bounds but prevents maximum multipliers structurally preventing assumptions.
- **`stale` (Confidence: 0.0)**: Timestamps slipping >300s backwards. Explicitly blocks execution, dropping naive calculations representing `missing` limits preventing heavy assumptions across wildly volatile nodes.
- **`missing` (Confidence: 0.0)**: Total data outage tracking parameters reliably. Replays strictly fail holding operations mapping to these states directly.
- **`rate_limited`**: Signals exact limits representing 429 logic. 

**Backfill Modifiers:**
If networks apply mathematical backfills executing off the chain, Confidence scores are slashed by 50% explicitly representing non-native oracle traces inherently isolating external manipulation algorithms cleanly.
