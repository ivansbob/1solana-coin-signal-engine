# The Abyssal Zone

Upgrades categorized under "Abyssal Defenses" are explicitly designed constraints blocking execution on tokens that statistically exhibit properties resulting in terminal velocity failures.

## 1. Late Smart Money Chasing (Overextended Entries)
If algorithms flag a token via generic momentum but trace the `smart_cohort_weighted_avg_entry_price` beyond distance gaps of 75% alongside bundle irregularities, the engine refuses operation entirely. Following 80% run-ups inherently guarantees holding the bag for early sniper clusters.

## 2. Hard Bundle Dominance
Tokens surviving exclusively due to continuous simulated bundle-bidding receive severe metric downgrades. The signal engine is calibrated purely for authentic organic volume separation. Total Score modifiers directly degrade linearly corresponding to inorganic ratios.
