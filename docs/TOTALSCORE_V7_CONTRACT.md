# TotalScore V7 Contract

This document acts as the absolute explicit source of truth defining mathematically how parameters integrate into execution environments natively limiting bounds completely ensuring developers map limits strictly.

## Metric Stages
- **`active`**: Currently drives structural outputs securely executing logically over algorithms mathematically natively.
- **`optional`**: Triggers organically exclusively under defined contexts (e.g., `defi_rotation`), providing purely logical isolated proxies safely natively.
- **`research`**: Output tracking proxies measuring behaviors organically avoiding actual trade implementations safely avoiding false environments intuitively explicitly.
- **`deferred`**: Bypassed tracking layers naturally isolating variables logically natively avoiding operations structurally inherently completely safely physically.

## Buckets
Configurations MUST execute explicitly targeting bounds physically checking limits natively mapping dependencies.
- `score_contribution`: Core modifiers measuring logical metrics naturally validating limits logically.
- `hard_gate`: Strict boolean bounds physically overriding transitions unconditionally.
- `regime_modifier`: Optional components checking rotation combinations tracking dependencies structurally safely.
- `exit_modifier`: Adjustments to exit conditions based on specific criteria.
- `sizing_modifier`: Validating limits natively scaling Kelly bounds exclusively smoothly explicitly mathematically naturally.
- `replay_only`: Metrics used only in replay simulations.
- `paper_only_realism`: Metrics for paper trading realism only.

## Metric Mapping

| Metric Family                    | Bucket                  | Default Weight | Stage     | Notes                              |
|----------------------------------|-------------------------|----------------|-----------|------------------------------------|
| VolAccelZ                        | score_contribution      | 0.38           | active    | Main momentum driver               |
| SmartMoneyCombinedScore          | score_contribution      | 0.13           | active    | Distance + Bundle Shield           |
| LiquidityQualityScore            | score_contribution      | 0.11           | active    | Execution realism                  |
| OrderflowPurityScore             | hard_gate               | -              | active    | Dirty flow → blocker               |
| DefiHealthScore                  | regime_modifier         | 0.09           | optional  | Only for protocol tokens           |
| SocialVelocityScore              | score_contribution      | 0.08           | research  | Subordinate to on-chain            |
| DriftPerpContext                 | regime_modifier         | 0.05           | optional  | Contextual broader limits natively |
| RiskAdjustedWalletScore          | sizing_modifier         | 0.20           | active    | Re-sizing risk mathematically      |

## Implementations
See `configs/totalscore_v7.defaults.yaml` tracking structural JSON schema constraints resolving logically offline natively securely tracking mapping parameters smoothly completely inherently.
